# Image Manager Assignment

Small Express + React app for uploading and replacing images in backend/public.

Run backend: cd backend && npm install && node server.js
Run frontend: cd frontend && npm install && npm start
